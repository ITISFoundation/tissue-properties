[bumpversion]
current_version = 1.0.8
commit = False
message = service version: {current_version} → {new_version}
tag = False

[bumpversion:file:.osparc/tissue-properties/metadata.yml]
search = {current_version}
replace = {new_version}

[bumpversion:file:Makefile]
search = {current_version}
replace = {new_version}

[bumpversion:file:docker-compose-local.yml]
search = {current_version}
replace = {new_version}

[bumpversion:file:docker-compose-development.yml]
search = {current_version}
replace = {new_version}
